server:
  port: 8080
  servlet:
    context-path: /

spring:
  datasource:
    url: jdbc:postgresql://ep-aged-poetry-alxzcrru.c-3.eu-central-1.aws.neon.tech:5432/neondb?sslmode=require
    username: neondb_owner
    password: npg_Cbpzvea8A1qn
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000

  jpa:
    hibernate:
      ddl-auto: update          # use 'validate' in production
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    open-in-view: false

  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai
    default-property-inclusion: non_null

# JWT Configuration
jwt:
  secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970   # 64-char hex secret, change in production!
  expiration: 86400000        # 24 hours in milliseconds
  refresh-expiration: 604800000  # 7 days in milliseconds

# Mock Payment Configuration
payment:
  mode: mock   # 模拟支付，无需真实支付网关

# App Config
app:
  # 模拟支付成功后跳转回 H5（mobile-frontend 默认 5174）
  frontend-url: http://localhost:5174
  cors-allowed-origins:
    - http://localhost:5173
    - http://localhost:5174
    - http://localhost:3000
    - http://127.0.0.1:5173
    - http://127.0.0.1:5174
    - http://127.0.0.1:8000

logging:
  level:
    com.easycode: DEBUG
    org.springframework.security: INFO
